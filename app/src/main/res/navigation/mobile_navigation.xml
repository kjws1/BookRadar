<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.bookradar.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_nav_home_to_nav_book_info"
            app:destination="@id/nav_book_info" />
    </fragment>

    <fragment
        android:id="@+id/nav_borrow"
        android:name="com.example.bookradar.ui.gallery.GalleryFragment"
        android:label="@string/menu_borrow"
        tools:layout="@layout/fragment_gallery" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="com.example.bookradar.ui.slideshow.SlideshowFragment"
        android:label="@string/menu_settings"
        tools:layout="@layout/fragment_slideshow" />
    <fragment
        android:id="@+id/nav_book_info"
        android:name="com.example.bookradar.BookInfoFragment"
        android:label="@string/nav_book_info"
        tools:layout="@layout/fragment_book_info">
        <argument
            android:name="item"
            app:argType="com.example.bookradar.model.DocumentModel"
            app:nullable="true"
            android:defaultValue="@null" />
    </fragment>


</navigation>